# Runtime display traffic uses the board's existing CDC ACM function. ZMK
# logging remains off so diagnostic text can never enter the binary stream.
CONFIG_SPI=y
CONFIG_SERIAL=y
CONFIG_USB_DEVICE_STACK=y
CONFIG_USB_CDC_ACM=y
CONFIG_UART_LINE_CTRL=y
CONFIG_USB_CDC_ACM_RINGBUF_SIZE=2048
