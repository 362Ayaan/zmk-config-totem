CONFIG_SPI=y
CONFIG_GPIO=y

# This target deliberately bypasses LVGL, the Zephyr ST7789 driver, QSPI pet
# storage, and the pet uploader. It tests only the XIAO pins, raw SPI, and the
# panel's standard power-on command sequence.
CONFIG_ZMK_DISPLAY=n
