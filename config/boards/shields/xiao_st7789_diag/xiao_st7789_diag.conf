CONFIG_GPIO=y

# This target deliberately bypasses LVGL, the Zephyr ST7789/SPI drivers, QSPI
# pet storage, and the pet uploader. It tests direct XIAO GPIO signaling and
# the panel's standard power-on command sequence.
CONFIG_ZMK_DISPLAY=n
